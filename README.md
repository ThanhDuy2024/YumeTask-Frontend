MS ToDo - React + Tailwind (sample)

Files inside src/ are ready to paste into a Vite project.

Quick start:
  1. npm install
  2. npx tailwindcss init -p
  3. ensure tailwind.config.js content points to ./index.html and ./src/**/*.{js,jsx}
  4. npm run dev
